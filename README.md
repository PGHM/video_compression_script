# PowerShell video compression script

Very awesome and probably horrible (but working) PowerShell script to compress videos while maintaining the original metadata

Uses Handbrake and ffmpeg as dependencies.
